# 日本語版（原典）— 執筆規約

> 本書を**読む**人はここではなく [README.md](README.md) へ。これは寄稿者向けの執筆メモ。
> 全体の作業規約は [/CLAUDE.md](../../CLAUDE.md)、英語版の規約は [../en/STYLE.md](../en/STYLE.md)。

`book/ja/` は ministan の**原典**。英語版 `book/en/` はこれを起点にトランスクリエーションする。

## 声・トーン

- **やさしい基礎編／形式は応用編**で少しだけ。型理論を前提にしない。
- 一貫して **non-rejecting**（[/CLAUDE.md](../../CLAUDE.md) の「軸」）を縦糸に通す。
- Carroll モチーフ（Wonderland / Looking-Glass）は意図的。やさしくカジュアルな文体を壊さない（硬くしない）。
- 各章は「動くものを少しずつ育てる」。章末に「まとめ／簡略化」、次章への引き。

## 用語・カタカナ表記（rigor 台帳に準拠）

判定軸は文字種でなく**対象読者（プログラマー）の定着度・明快さ・誤解リスク**。定着カタカナは主表記、
初出で別訳／英語を1回併記。一次資料は
`/Users/megurine/repo/site/rigor.typedduck.fail/docs/ja/`（`translation-glossary`・
`katakana-longvowel-ledger`・`term-audit`）。

- **context ＝「コンテキスト」**（主表記）。**初出のみ「コンテキスト（文脈）」**と漢訳を補足、以降は素の
  「コンテキスト」（初出は `wonderland/02-scope.md`）。「文脈」は初出 gloss を除き使わない。
- **union type ＝「ユニオン型」**（初出 05 に「ユニオン型（union type、合併型とも）」。「合併型」は初出 gloss だけ）。
- **gradual typing ＝「漸進的型付け」**（初出 README に「漸進的型付け（gradual typing）」）。
- **narrowing ＝「絞り込み（narrowing）」のまま**保留（rigor は「ナローイング」だが、硬くなるため
  ユーザー判断で「絞り込み」を維持）。

**カタカナ長音**: `-er`/`-or`/`-ar`/`-ure` で**短縮形が工学定着**の語は `ー` を落とす
（DROP: パーサ・アクセサ・パラメータ・ハンドラ・リゾルバ・フォーマッタ・イテレータ・コンテナ 等）。
未定着・一般語・行為者名詞、`-ry`/`-ly`/`-ory` は**伸ばす**
（KEEP: メンバー・レシーバー・サーバー・バリデーター・レイヤー 等）。**コード／インラインコード内は対象外。**
迷う `-er`/`-or`/`-ar` は rigor 台帳を引く。新章・新語も同台帳で判定。

## 約物（全角ダッシュ）

- **2倍ダーシ ` —— `**（U+2014 を2つ・前後スペース）に統一。`―`（U+2015）は使わない。
- **置換は perl でなく Python**（多バイト破損・`\\1`→`\x01` の罠）。詳細・検証 grep は
  [/CLAUDE.md](../../CLAUDE.md) の「約物の罠」。

## 参考書メモ（chibirigor 流・任意の副読本）

型理論を一段深く覗きたい読者向けに、**必読でない／本筋に混ぜない**前提で添える:

- まえがき `README.md` に **`## 参考書（任意）`** 節（**『しくみ』**＝遠藤『型システムのしくみ』／
  **TAPL**＝Pierce『型システム入門』）。
- 各章頭（スナップショット注記の直後）に **`> 参考書（任意）：…`** で TAPL/『しくみ』の対応章マップ＋一言。
  要所は本筋に **`> 参考書メモ：…`** をインライン。
- トーン: 対応章の明示・**相違の正直な明記**（『しくみ』が避けた無タグ union、教科書に対応章なし＝S4
  フロー解析 等）・理論的裏付け。**名前落とし禁止・型理論を前提にしない軸は不変。**
- 表記: **『しくみ』N章**（空白なし）・**TAPL N 章**（空白あり）。
- **型理論の薄い章（Part 1・9・S1・S6）は付けない。** 新章を足したら同じ要領で添える。
- 対応マップの一次資料は chibirigor `docs/20260607-type-systems-distilled-rigor-mapping.md`。

## コードリンク・図版

- コードリンク方針（本文は各章スナップショット `impls/<vol>/NN-*` を指す）は [/CLAUDE.md](../../CLAUDE.md)。
- 図版（SVG・light/dark・`<picture>`・カラーマップ・検証）は [/docs/figures.md](../../docs/figures.md)。
  日本語版の図キャプションは日本語、約物は本文同様 ` —— `。
